import { ContactFormPageContent } from '@features/contact/components';

const ContactFormPage = () => {
  return (
    <main className="flex flex-col bg-slate-100 min-w-max h-max min-h-screen items-center">
      <ContactFormPageContent />
    </main>
  );
};

export default ContactFormPage;
